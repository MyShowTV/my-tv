name: IPTV Proxy Sync

on:
  schedule:
    - cron: "0 */2 * * *"   # 每 2 小时执行一次
  workflow_dispatch:          # 必须保留，用于手动点击运行按钮

jobs:
  sync_job:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 检出代码
        uses: actions/checkout@v3

      - name: ⚙️ 安装代理引擎并寻找台湾节点
        run: |
          wget -q https://github.com/MetaCubeX/Mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip mihomo-linux-amd64-v1.18.9.gz
          chmod +x mihomo-linux-amd64-v1.18.9

          SUCCESS=false
          # 轮询探测端口 17001 到 17015
          for p in $(seq 17001 17015); do
            echo "-----------------------------------"
            echo "🔁 尝试端口: $p"
            
            cat <<EOF > config.yaml
mixed-port: 7890
allow-lan: true
mode: rule
log-level: info
proxies:
  - name: "TW-NODE"
    type: ss
    server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
    port: $p
    cipher: aes-256-gcm
    password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
    udp: false
proxy-groups:
  - name: "SSRDOG"
    type: select
    proxies: ["TW-NODE"]
rules:
  - DOMAIN-SUFFIX,ofiii.com,SSRDOG
  - DOMAIN-SUFFIX,litv.tv,SSRDOG
  - DOMAIN,ip-api.com,SSRDOG
  - MATCH,DIRECT
EOF

            ./mihomo-linux-amd64-v1.18.9 -f config.yaml > mihomo.log 2>&1 &
            sleep 25

            # 归属地校验 (必须是 TW)
            CHECK=$(curl -s --max-time 15 -x http://127.0.0.1:7890 http://ip-api.com/json/ || echo '{"countryCode":"FAILED"}')
            COUNTRY=$(echo $CHECK | sudo apt-get install -y jq > /dev/null && echo $CHECK | jq -r '.countryCode' 2>/dev/null || echo "ERROR")
            IP=$(echo $CHECK | jq -r '.query' 2>/dev/null || echo "UNKNOWN")
            
            echo "🌐 出口 IP: $IP | 归属地: $COUNTRY"

            if [[ "$COUNTRY" == "TW" ]]; then
              echo "✅ 锁定台湾节点成功！使用端口: $p"
              SUCCESS=true
              break
            else
              echo "❌ 非台湾节点，尝试下一个..."
              pkill -f mihomo || true
              sleep 2
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ 失败：未能在可用端口中找到台湾 IP。"
            exit 1
          fi

      - name: 🐍 配置 Python 环境
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: 📦 安装抓取依赖
        run: |
          python -m pip install --upgrade pip
          pip install blinker==1.7.0 requests selenium-wire==5.1.0 chromedriver-autoinstaller selenium==4.9.1

      - name: 🚀 执行同步脚本
        run: python cloud_sync.py

      - name: 🧾 打印日志摘要
        if: always()
        run: tail -n 20 mihomo.log || true
