name: IPTV Proxy Sync

on:
  schedule:
    - cron: "0 */4 * * *"   # 每 4 小时运行一次
  workflow_dispatch:          # 支持手动点击运行

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Mihomo & Find Taiwan IP
        run: |
          # 下载并解压 Mihomo (Clash Meta)
          wget -q https://github.com/MetaCubeX/Mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip mihomo-linux-amd64-v1.18.9.gz
          chmod +x mihomo-linux-amd64-v1.18.9

          SUCCESS=false
          # 循环探测端口 17001 到 17015
          for p in $(seq 17001 17015); do
            echo "-----------------------------------"
            echo "🔁 正在测试端口: $p"
            
            # 动态生成带分流规则的配置
            cat <<EOF > config.yaml
mixed-port: 7890
allow-lan: true
mode: rule
log-level: info
proxies:
  - name: "TW-NODE"
    type: ss
    server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
    port: $p
    cipher: aes-256-gcm
    password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
    udp: false
proxy-groups:
  - name: "SSRDOG"
    type: select
    proxies: ["TW-NODE"]
rules:
  - DOMAIN-SUFFIX,ofiii.com,SSRDOG
  - DOMAIN-SUFFIX,litv.tv,SSRDOG
  - DOMAIN,ip-api.com,SSRDOG
  - MATCH,DIRECT
EOF

            # 启动代理
            ./mihomo-linux-amd64-v1.18.9 -f config.yaml > mihomo.log 2>&1 &
            sleep 25

            # 验证归属地 (必须是 TW)
            CHECK=$(curl -s --max-time 15 -x http://127.0.0.1:7890 http://ip-api.com/json/ || echo '{"countryCode":"FAILED"}')
            COUNTRY=$(echo $CHECK | jq -r '.countryCode' 2>/dev/null || echo "ERROR")
            IP=$(echo $CHECK | jq -r '.query' 2>/dev/null || echo "UNKNOWN")
            
            echo "🌐 当前出口 IP: $IP | 归属地: $COUNTRY"

            if [[ "$COUNTRY" == "TW" ]]; then
              echo "✅ 锁定台湾节点成功！使用端口: $p"
              SUCCESS=true
              break
            else
              echo "❌ 地区不匹配，杀掉进程并尝试下一个..."
              pkill -f mihomo || true
              sleep 2
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ 错误：所有尝试的端口均非台湾 IP，请检查机场节点状态。"
            exit 1
          fi

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: 📦 Install dependencies
        run: |
          sudo apt-get install -y jq
          python -m pip install --upgrade pip
          pip install blinker==1.7.0 requests selenium-wire==5.1.0 chromedriver-autoinstaller selenium==4.9.1

      - name: 🚀 Execute Sync Script
        run: python cloud_sync.py

      - name: 🧾 Show Final Logs
        if: always()
        run: |
          echo "=== 代理日志最后 20 行 ==="
          tail -n 20 mihomo.log || true
