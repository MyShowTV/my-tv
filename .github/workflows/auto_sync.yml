name: IPTV Proxy Sync

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Mihomo (Auto Port Selection)
        run: |
          wget -q https://github.com/MetaCubeX/Mihomo/releases/download/v1.18.9/mihomo-linux-amd64-v1.18.9.gz
          gunzip mihomo-linux-amd64-v1.18.9.gz
          chmod +x mihomo-linux-amd64-v1.18.9

          # 自动检测通的端口
          SUCCESS=false
          for p in $(seq 17001 17010); do
            echo "🔁 尝试端口: $p"
            
            # 生成临时配置
            cat <<EOF > config.yaml
          mixed-port: 7890
          allow-lan: true
          mode: global
          log-level: info
          proxies:
            - name: "TW-AUTO"
              type: ss
              server: brtlee3ogRTfxtWt.xf9pzeslxw.sbs
              port: $p
              cipher: aes-256-gcm
              password: 1d23e3c5-c047-46a0-b7d8-d838aff7ae8d
              udp: false
          EOF

            # 启动并等待
            ./mihomo-linux-amd64-v1.18.9 -f config.yaml > mihomo.log 2>&1 &
            sleep 20

            # 验证 IP
            IP=$(curl -s --max-time 10 -x http://127.0.0.1:7890 https://ifconfig.me || echo "FAILED")
            echo "🌐 隧道出口 IP: $IP"

            if [[ "$IP" != "FAILED" && "$IP" != 52.* && "$IP" != 20.* ]]; then
              echo "✅ 代理连接成功: $IP (端口 $p)"
              SUCCESS=true
              break
            else
              echo "❌ 端口 $p 不通或被识别为机房 IP，尝试下一个..."
              pkill -f mihomo || true
              sleep 2
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "❌ 所有端口均失败，请检查机场是否屏蔽了 GitHub Actions"
            exit 1
          fi

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.9"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install blinker==1.7.0 requests selenium-wire==5.1.0 chromedriver-autoinstaller selenium==4.9.1

      - name: 🚀 Execute Sync Script
        run: python cloud_sync.py

      - name: 🧾 Show Logs
        if: always()
        run: |
          echo "=== Mihomo 日志摘要 ==="
          tail -n 20 mihomo.log || true
